/**
 * `sizes` hints for Services page imagery — keep in sync with Tailwind layout:
 * cards: 1 col < md, 2 cols in max-w-[1400px]; feature images: full width < lg, ~50% ≥ lg.
 */
export const SERVICE_CARD_IMAGE_SIZES =
  '(max-width: 767px) 100vw, (max-width: 1400px) 50vw, 650px'

export const SERVICE_FEATURE_IMAGE_SIZES =
  '(max-width: 1023px) 100vw, (max-width: 1400px) 50vw, 650px'
